spring:
  mvc.view:
    prefix: /WEB-INF/
    suffix: .jsp

moviefun:
  datasources:
    movies:
      jdbcUrl: jdbc:mysql://localhost:3306/movies?useSSL=false
      username: root
      password:
    albums:
      jdbcUrl: jdbc:mysql://localhost:3306/albums?useSSL=false
      username: root
      password: